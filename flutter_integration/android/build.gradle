buildscript {
    repositories {
        google()
        mavenCentral()  // Certifique-se de que o Maven Central está aqui
        maven { url "https://chaquo.com/maven" }  // Adicione este repositório
    }
    dependencies {
        classpath "com.android.tools.build:gradle:7.0.0"
        classpath "com.chaquo.python:gradle:9.1.0"  // Adicione a dependência do Chaquopy com a versão correta
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()  // Certifique-se de que o Maven Central está aqui
        maven { url "https://chaquo.com/maven" }  // Adicione este repositório
    }
}
